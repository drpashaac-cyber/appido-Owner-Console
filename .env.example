# APPIDO Owner Console — environment
#
# The console ships with an in-memory MockApi by default, so the demo/preview
# build runs with NO backend and NO env vars required.
#
# To connect the real backend, implement HttpApi (see BACKEND.md §9) and set:
VITE_API_BASE_URL=https://api.appido.io
